import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gorantlasadwik.synctunes',
  appName: 'Sai Ram Kirana',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
